<!DOCTYPE html>
<html>
<head>
  <title>Splurty</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>

  <header>
  	<div class="col-xs-3">
      <%= link_to root_path, :class => 'logo' do %>
  	    <div class="circle">
  	  	  <br />
  	  	  Give me
  	  	  <br />
  	  	  another
  	  	  <br />
  	  	  quote!
  	    </div>
      <% end %>
  	</div>
  	<div class="col-xs-4 hidden-xs slogan">
  	  Splurting out quotes randomly from the Game of Thrones TV series!
  	</div>
  	<div class="col-xs-8 col-xs-offset-1 col-sm-offset-0 col-sm-5 links">
      <!-- Link trigger modal -->
      <%= link_to 'Contribute', '#', :data => {:toggle => 'modal', :target => '#myModal'} %>
      &nbsp;&nbsp;
      <%= link_to 'About', about_path %>
  	</div>
  </header>

  <!-- Modal -->
  <div class="modal fade" id="myModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
    <div class="modal-dialog">
      <div class="modal-content">
        <div class="modal-header">
          <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
          <h4 class="modal-title" id="myModalLabel">Contribute your quote</h4>
        </div>
        <%= simple_form_for Quote.new do |f| %>
          <div class="modal-body">
            <%= f.input :saying, :label => "What is the quote you want to add?", :placeholder => "When you play the game of thrones, you win or you die." %>
            <br />
            <%= f.input :author, :label => "Which character said that quote?", :placeholder => "Cersei Lannister" %>
          </div>
          <div class="modal-footer">
            <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
            <%= f.submit 'Create', :class => 'btn btn-danger' %>
          </div>
        <% end %>
      </div>
    </div>
  </div>

  <hr />
  
  <br class="clear" />
  
  <div class="container">
    <% if flash[:error].present? %>
      <div class="col-xs-10 col-xs-offset-1 alert alert-danger">
        <%= flash[:error].html_safe %>
      </div>
    <% end %>

    <%= yield %>
  </div>

  <div class="footer">
    <div class="container">
      <div class="col-xs-12 col-sm-6">
        <%= link_to "Another quote, please!", root_path, :class => 'next-quote' %>
      </div>
      <div class="col-xs-12 col-sm-6 footer-blurb">
        Coded in California by Cynrick Chiu<br />
        Learning Ruby on Rails and enjoying every second of it
      </div>
    </div>
  </div>

</body>
</html>
